# pnpm v11 defaults to auto-installing before `pnpm run`; that would replace
# the link-local-dsh symlinks with real npm copies behind your back. Install
# explicitly instead.
verify-deps-before-run=false
